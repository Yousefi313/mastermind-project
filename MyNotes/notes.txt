There are 10 rows and each row has 4 circles.

Following points should be done next time:

1. User can only change the color of the first row. After clicking the check button
then the user will be able to change the colors of the second row.

2. There should be 6 colors to chose.
3. Use the 2D loop to make row and column(circles)